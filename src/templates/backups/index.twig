{% extends "enupalbackup/_layouts/elementindex" %}
{% set title = "Backups"|t %}
{% set elementType = 'enupal\\backup\\elements\\Backup' %}

{% set extraPageHeaderHtml %}
	<div class="buttons">
		<a class="btn submit add icon" href="{{ url('enupalbackup/backup/now') }}">{{ "Backup Now"|t('enupalbackup') }}</a>
		<a class="btn submit add icon" href="{{ url('enupalbackup/backup/restore') }}">{{ "Restore"|t('enupalbackup') }}</a>
	</div>
{% endset %}